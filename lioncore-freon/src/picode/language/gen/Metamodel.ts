// Generated by the ProjectIt Language Generator.
import { observable, makeObservable } from "mobx";
import {
    MobxModelElementImpl,
    observablelistpart,
    PiModelUnit,
    PiUtils,
    PiParseLocation,
    matchElementList,
    matchPrimitiveList,
    matchReferenceList
} from "@projectit/core";
import { MetamodelElement, LIonCoreMetaType } from "./internal";

/**
 * Class Metamodel is the implementation of the model unit with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to changes in the state of its properties.
 */
export class Metamodel extends MobxModelElementImpl implements PiModelUnit {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data
     */
    static create(data: Partial<Metamodel>): Metamodel {
        const result = new Metamodel();
        if (!!data.qualifiedName) {
            result.qualifiedName = data.qualifiedName;
        }
        if (!!data.name) {
            result.name = data.name;
        }
        if (!!data.elements) {
            data.elements.forEach(x => result.elements.push(x));
        }
        if (!!data.parse_location) {
            result.parse_location = data.parse_location;
        }
        return result;
    }

    fileExtension: string;
    readonly $typename: LIonCoreMetaType = "Metamodel"; // holds the metatype in the form of a string
    $id: string; // a unique identifier
    parse_location: PiParseLocation; // if relevant, the location of this element within the source from which it is parsed
    qualifiedName: string = ""; // implementation of qualifiedName
    name: string = ""; // implementation of name
    elements: MetamodelElement[]; // implementation of part 'elements'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = PiUtils.ID(); // uuid.v4();
        }
        makeObservable(this, { qualifiedName: observable });
        makeObservable(this, { name: observable });

        // both 'observablepart' and 'observablelistpart' change the get and set of an attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable
        observablelistpart(this, "elements");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    piLanguageConcept(): LIonCoreMetaType {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    piId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    piIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    piIsUnit(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    piIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    piIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Metamodel>): boolean {
        let result: boolean = true;
        if (
            result &&
            toBeMatched.qualifiedName !== null &&
            toBeMatched.qualifiedName !== undefined &&
            toBeMatched.qualifiedName.length > 0
        ) {
            result = result && this.qualifiedName === toBeMatched.qualifiedName;
        }
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && !!toBeMatched.elements) {
            result = result && matchElementList(this.elements, toBeMatched.elements);
        }
        return result;
    }
}
